# 하모니업 (Harmony Up) — 프로젝트 지침

## 개요

**2026-08-20 기준 스코프**: 원래 구상(화음 자동 생성 + 따라 부르기 채점)은 화음 소리 품질이 여러 라운드(93~115절, docs/CONCEPTS.md)를 거쳐도 안 풀려서, 사용자 결정으로 화음 API 전체를 삭제하고(116절) 멜로디 인식(녹음 → 음표 추출 → 악보 표시)부터 다시 다졌다(117~119절, 실기기 다회 재검증 완료). **120절부터 화음을 처음부터 재설계** — 합성음(순수 사인파, 120~122절)으로 화음 선택/타이밍/재생 구조(크로스페이드+overlap-add)부터 변수 격리해 검증한 뒤, 목소리 피치시프트 단계에서 WSOLA(123절, "어색함")를 거쳐 WORLD 보코더로 단계별 재통합(124~127절)해 **실기기에서 "훨씬 자연스럽다"는 확인을 받았다**. 채점 관련 소스(`PracticeView+Scoring.swift`, `PitchScorer`, `PracticeSummary`, `PracticeAttempt`, `HistoryView`)는 여전히 UI에서 안 보인다 — 화음 재생이 다시 자리잡은 지금부터 다시 붙일지 논의 대상.

- 전체 제품 배경(문제의식, 경쟁 분석, 페르소나, 성공지표 등, 화음 스코프 포함 원래 구상): `docs/prd.md`
- 진행 상황 체크리스트: `docs/PROGRESS.md`, 신호처리/설계 결정 히스토리: `docs/CONCEPTS.md`(특히 116절이 화음 제거 결정 배경)

세션 시작 시 위 문서들, 특히 `docs/CONCEPTS.md` 최신 절을 참고해서 지금 스코프를 정확히 파악할 것 — `docs/prd.md`는 화음 기능까지 포함한 원래 구상이라 지금 코드 상태와 다를 수 있다. 큰 방향을 바꿔야 할 것 같으면 먼저 알리고 진행할 것.

## 기술 스택 및 제약

- Swift, iOS 앱 (SwiftUI)
- 오디오 캡처: AVAudioEngine, AVAudioSession
- 피치 검출: YIN 알고리즘 직접 구현 (Accelerate/vDSP 기반)
  - 서드파티 피치 검출 라이브러리(AudioKit PitchTap 등) 사용 금지 — 학습 목적상 직접 구현이 원칙. 정확도 비교용 참고 자료로만 사용 가능.
- 데이터 저장: SwiftData
- 서버/백엔드 없음 — 전 과정 온디바이스 처리가 기본 원칙
- 외부 유료 API(Klangio 등)는 v1 범위에서 사용하지 않음

## 현재 단계: 화성 모델 v2(HMM+Viterbi) 재구현 완료 — 다음은 실기기 청취 검증

지금 살아있는 파이프라인: `AudioCapture`(마이크) → `QuickRecordView`(최대 60초 녹음) → `MelodySegmenter`(배치 분석, 음표별 음높이/시작시각/길이 추출) → `RecordingAnalyzer`(`KeyDetector`로 조성 판별 + `ChordGenerator`로 스텝별 화음 계산) → `MelodyStep` 배열(harmony 포함) → `VexFlowScoreView`(멜로디 단일 오선보로 표시, **주의: 지금 멜로디+화음 내용과 안 맞는 상태로 확인됨, 128절에서 발견했지만 아직 미수정** — 다음에 손볼 대상) + 조작부(`captureHero`)의 재생 버튼들: "화음 듣기"(합성음, `SynthesizedHarmonyTrackBuilder`) / "내 목소리로 화음"(WORLD, `VoiceHarmonyTrackBuilder`) / 성부별 솔로 4개 / 재생 중 성부 뮤트 토글 4개. `TonePlayer`는 녹음 전 "첫음 잡기" 참고음 재생에만 쓰인다.

실기기 검증(121~122절)에서 "따다다닥 끊김"(화음이 멜로디 음마다 바뀌는데 매번 무음까지 페이드했던 게 원인 — 진짜 크로스페이드로 수정)과 "지지직"(짧은 음 램프 중첩 + 화음 재생 음량이 스피커 재생 한계에 가까웠던 것으로 추정 — 램프 겹침 방지+음량 하향으로 개선)을 순서대로 고쳤다. 이어서 실제 노래로 "화음이 애매하다" 재제보 — 콘솔 로그로 진단한 결과 화음 계산 자체는 정확했고, 원인은 `ChordGenerator` v1(멜로디 음마다 새 근음)의 화성 모델 한계로 확인(v2 재검토는 나중 과제로 미룸).

**123절, 2단계 1차 시도(WSOLA)**: `SynthesizedHarmonyTrackBuilder`와 같은 크로스페이드 구조 위에 목소리 피치시프트(WSOLA)를 얹은 `VoiceHarmonyTrackBuilder` 추가, "내 목소리로 화음" 버튼 추가. 실기기 청취 결과 "둘 다 어색하네"(합성음/WSOLA)로 확인 — 화음이 애매하다는 재제보를 로그로 진단해 화성 모델(v1) 한계임도 별도로 확인(코드 버그 아님, 나중 과제).

**124~127절, WORLD로 교체(성공)**: 웹서비스/오픈소스 라이선스 조사(상업용 iOS에 안전한 건 SoundTouch/WORLD) 후, 사용자 요청대로 **한번에 안 넣고 3단계로 나눠** WORLD 보코더를 복원 — 1단계(벤더링+빌드 확인) → 2단계(Swift 래퍼+합성 신호 피치 정확도 유닛테스트) → 3단계(`VoiceHarmonyTrackBuilder` 연결), 매 단계 빌드+테스트로 확인. 실기기 청취 결과 **"아까보다 훨씬 자연스럽네"로 확인(127절)** — WSOLA 대비 뚜렷한 개선.

**128절, 멜로디+화음 자연스러움 7단계 다듬기**: "딱딱 끊긴다" 제보 → (1)멜로디도 세그먼트로 잘려 미인식 구간이 무음 되던 문제를 원본 패스스루로, (2)화음 크로스페이드 확장분이 이웃 음을 침범하던 문제를 자기반사(`VoiceHarmonyTrackBuilder.mirrorExtendedSlice`)로 수정 → (3~4)포먼트 차이(0.95/1.05, 95절보다 보수적) 재도입 + 화음 3성부 backingGain(0.65)으로 멜로디 강조 → (5~6)성부별 솔로 재생 버튼 복구, 재생 버튼 전체가 실수로 악보 카드 안에 있던 걸 조작부(`captureHero`)로 재배치, 재생 중 성부 뮤트(즉시 재시작 방식, 세대 토큰으로 경쟁 상태 방지) → (7)크로스페이드 20ms→40ms 확대. 유닛테스트 146개, 매 단계 실기기 확인, "잘 작동해"/"괜찮네"로 마무리.

**129~131절, "전체 한 번 분석+F0곡선 재합성" 구조 개선(완료)**: `HarmonyUpWorldBridge.cpp`를 analyze/synthesize 핸들 기반 API로 분리(129절) → `VoiceHarmonyTrackBuilder`를 F0 곡선 조립+단일 재합성 방식으로 재작성, 세그먼트별 개별분석+진폭 크로스페이드 이어붙이기 구조 완전 제거(130절, 인접 스텝은 F0 선형보간 글리산도로 전환) → 실기기 청취 **"전보다 자연스러워서 좋다"로 목표 달성 확인**(131절).

**132절, 보코더 음질(기계음) 개선 1차 실험**: 남은 "기계음" 느낌에 대해 WORLD `D4C`의 `threshold`(비주기성 추정 임계값, 기본 0.85)를 화음 성부 분석에서만 낮춰봄 — 0.0(지름길 완전히 끔)은 "지지직" 잡음이 생겨 과했고, 중간값 **0.5로 절충 확정**("나쁘지 않아").

**133절, 다음 과제 — 화성 모델 v2 부활 검토(미착수)**: 이어서 "화음이 고정된 느낌, 멜로디 느낌이 강한 화음이면 좋겠다"는 피드백 → AskUserQuestion으로 "화음 진행 자체가 단순반복"으로 구체화. 원인은 현재 v1(101~102절)이 멜로디 음마다 그 음 자신을 근음으로 매번 새로 화음을 계산해서 사실상 병행진행에 가깝기 때문 — 51절 v2(HMM+Viterbi, 코드가 문맥에 맞게 여러 음에 걸쳐 유지)를 되살리는 방향을 제안, 구두 합의만 하고 세션 종료(저장 후 `/clear`). **119절에서 이미 "101절의 v1 회귀 이유(박자 안 맞음)가 실은 크로스페이드 버그였을 가능성"을 진단해뒀고, 그 버그는 121·128~132절에서 전부 해결됨** — 다음 세션 시작 시 이 제안을 다시 확인받고 착수할 것(51절 방출점수/전이점수/Viterbi 설계를 지금 배치 구조에 맞게 재구현 필요, git 그대로 복원은 어려움).

**134절, 화성 모델 v2 재구현 완료**: 다음 세션 시작 시 AskUserQuestion으로 재확인("바로 착수") 후 진행. `ChordGenerator.harmonizeSequence` 내부를 51절 설계(방출점수+전이점수+Viterbi DP/backtrace) 기준으로 새로 작성 — 시그니처는 그대로, 온음계 밖 음은 "직전 유효 화음 이어받기" 계약도 그대로 유지(방출점수만 중립 0으로 둬서 코드 흐름은 안 끊기게). 테스트를 손으로 미리 계산해 만들었다가 두 번 틀렸다(C→F, C-E-G가 각각 공통 코드(IV/I)로 설명 가능해서 "코드 유지"가 오히려 정답이었음) — 실제 실행 결과 기준으로 재설계해 통과시켰다(`ChordGeneratorTests` v1 전용 테스트 교체, `MelodySessionTests` 기대값 수정). 유닛테스트 152개 아이패드 시뮬레이터 통과. **다음: 실기기(Ian) 청취 검증** — "화음이 고정된 느낌"이 실제로 해소됐는지 확인 필요.

채점(3도/5도/베이스 목표음 따라 부르기)은 화음 재생이 완전히 자리잡은 뒤 다시 붙일 대상 — `PitchScorer`/`PracticeAttempt`/`HistoryView`는 남아있지만 지금은 UI에서 안 쓰인다.

## 코딩 컨벤션

- 신호처리 핵심 로직에는 "왜 이렇게 했는지"를 짧은 주석으로 남길 것 (자기상관 윈도우 크기, 임계값 선택 근거 등)
- 매직 넘버 대신 명명된 상수 사용
- 신호처리 함수는 유닛 테스트가 가능하도록 순수 함수로 분리 — **View의 `@State`에 묶인 메서드 안에 신호처리/조합 로직을 직접 두지 말 것.** 화음 오디오 파이프라인(`harmonizedTrack`)이 한동안 `PracticeView` 확장 메서드로 있었는데, 그 조합 로직(간격 처리, 여러 음 이어붙이기)에 실제 버그(트랙 길이가 미묘하게 어긋나 "노래가 빠를수록 화음이 밀림")가 있었지만 유닛테스트가 없어서 실기기 청취로만 드러났다 — `PitchShifter`/`VoiceDoubler`/`AudioGain`처럼 이미 옆에 있던 순수 함수들은 같은 기간 내내 한 번도 실패하지 않았다. 이후 `HarmonyTrackBuilder`(PitchEngine)로 뽑아 유닛테스트를 붙였다 — 앞으로 이 자리를 다시 손댈 때는 그 테스트에 불변식(주로 길이 보존)을 먼저 추가해 통과시킨 뒤 실기기로 넘어갈 것.
- 오디오/신호처리 쪽을 실기기 청취로 튜닝할 때는 한 커밋에 여러 개념을 동시에 바꾸지 말 것 — 어느 변경이 무엇을 고쳤는지/깼는지 추적이 어려워진다. 복잡한 해법(예: 크로스페이드, 여러 스텝 묶기)보다 단순한 해법을 먼저 시도할 것 — 복잡도 자체가 새 버그의 원인이 될 수 있다(2026-08-20, 화음 파이프라인 7라운드 조정에서 실제로 겪음).

## 작업 방식

- 작업자는 iOS 개발은 익숙하지만 DSP는 이번에 처음 제대로 배우는 중이다. 코드만 작성하지 말고, 핵심 알고리즘 결정마다 왜 그렇게 했는지 1~2문장으로 짧게 설명할 것.
- 큰 구조 변경이나 새 의존성 추가 전에는 먼저 접근 방식을 요약해서 확인받을 것.
- 컴포넌트/기능을 하나씩 순서대로 만들고, 매 단계마다 결과를 확인받은 뒤 다음으로 넘어간다(한 번에 몰아서 만들지 않는다).
- 새 컴포넌트를 만들 때마다 그 안에서 쓴 기술/개념을 `docs/CONCEPTS.md`에 이해하기 쉽게 정리해서 이어붙인다.

## 작업 완료 루틴 (매번, 요청 없이도 자동으로)

작업 단위(컴포넌트 하나, 기능 하나) 하나를 마칠 때마다 아래를 전부 수행한다:

1. **`docs/CONCEPTS.md` 갱신** — 새로 쓴 기술/개념 설명 추가
2. **`docs/PROGRESS.md` 갱신** — 진행 상황 체크리스트 항목 추가(README.md가 아님 — 진행 상황이 너무 길어져 2026-08-20에 분리됨). `README.md`는 개요/기술스택/구성 요소 표/개발 명령어만 유지하고, 진행 상황은 링크 + 최근 작업 한 줄 요약만 갱신
3. **git commit + push** — 작업 단위별로 커밋을 나누고 원격 저장소에 푸시까지 완료
4. **Claude 메모리 갱신** — `project_harmonyup` 메모리에 이번 작업으로 달라진 진행 상황 기록

이 프로젝트는 홈 디렉토리 전체를 감싸는 상위 git 저장소와 별개로, `~/Desktop/HarmonyUp` 자체가 독립된 git 저장소다. push 전에는 항상 이 저장소 범위(`git status`가 프로젝트 파일만 보여주는지) 안에서 작업 중인지 확인할 것 — 상위 저장소로 잘못 올라가는 일이 없도록 주의.

## 세션 관리 — /compact 대신 /clear

- `/compact`(대화 요약 압축)는 쓰지 않는다 — 긴 코드/에러 로그/미세 조율 내용이 요약 과정에서 생략·왜곡되기 쉽고, 잘못 요약된 맥락을 사실로 믿고 계속 진행하면 환각(hallucination)으로 이어질 위험이 있다. 토큰 절감 효과도 크지 않다(요약본 자체도 계속 토큰을 차지해서 얼마 안 가 다시 한도에 도달).
- 대신 이 흐름을 따른다: 기능 하나가 구현되거나 버그가 잡히면 그때그때 **git commit**(+push)을 해두고, 한 단계(예: 어떤 Phase/Task)가 끝나면 고민 없이 **`/clear`**로 대화를 깨끗하게 초기화한 뒤 다음 작업을 새로 지시받는다.
- `/clear`는 대화 맥락만 비우는 것이지 소스 코드 파일이나 git 커밋 기록을 지우지 않는다 — 세션이 새로 시작해도 이 `CLAUDE.md`와 `docs/CONCEPTS.md`, git 로그만 다시 읽으면 필요한 맥락은 그대로 복원된다. 그래서 중요한 프로젝트 규칙/결정은 반드시 이 문서(또는 `docs/CONCEPTS.md`)에 남겨야 한다 — 대화 맥락에만 의존하지 말 것.
