# 하모니업 (Harmony Up) — 프로젝트 지침

## 개요

**2026-08-20 기준 스코프**: 원래 구상(화음 자동 생성 + 따라 부르기 채점)은 화음 소리 품질이 여러 라운드(93~115절, docs/CONCEPTS.md)를 거쳐도 안 풀려서, 사용자 결정으로 화음 API 전체를 삭제하고(116절) 멜로디 인식(녹음 → 음표 추출 → 악보 표시)부터 다시 다졌다(117~119절, 실기기 다회 재검증 완료). **120절부터 화음을 처음부터 재설계** — 합성음(순수 사인파, 120~122절)으로 화음 선택/타이밍/재생 구조(크로스페이드+overlap-add)부터 변수 격리해 검증한 뒤, 목소리 피치시프트 단계에서 WSOLA(123절, "어색함")를 거쳐 WORLD 보코더로 단계별 재통합(124~127절)해 **실기기에서 "훨씬 자연스럽다"는 확인을 받았다**. **136절에 채점이 다시 붙었다** — 다만 예전 방식(목표음 하나를 붙잡고 지속 발성하는 실시간 프레임 채점)이 아니라, "성부 하나를 먼저 들어보고 → 소리를 끄고 → 그 성부 한 소절을 통째로 불러서 배치 채점"하는 흐름으로 재설계했다(사용자 결정: "우리 앱의 목표는 멜로디로 화음 한 소절을 뽑아내면 그 화음 한 소절을 연습하는 것"). 기록 탭도 녹음 세션 단위로 갈아엎었다.

- 전체 제품 배경(문제의식, 경쟁 분석, 페르소나, 성공지표 등, 화음 스코프 포함 원래 구상): `docs/prd.md`
- 진행 상황 체크리스트: `docs/PROGRESS.md`, 신호처리/설계 결정 히스토리: `docs/CONCEPTS.md`(특히 116절이 화음 제거 결정 배경)
- **파일 구조: `docs/ARCHITECTURE.md`** — 2026-08-24에 junCook(ValleyRisk)의 MVVM 레이아웃으로 옮겼다(`HarmonyUp/Sources/{App,Core,Data,Domain,Features}`). 그 전 문서·주석에 나오는 `PitchEngine/`, `Views/`, `DesignSystem/` 경로는 전부 옛 자리다 — 타입 이름은 그대로이니 이름으로 찾을 것. 새 파일을 만들 때는 기능 우선 원칙을 따른다: 여러 기능이 공유하면 `Core`/`Domain`, 한 기능 전용이면 그 기능의 `Features/<기능>/Model`.

세션 시작 시 위 문서들, 특히 `docs/CONCEPTS.md` 최신 절을 참고해서 지금 스코프를 정확히 파악할 것 — `docs/prd.md`는 화음 기능까지 포함한 원래 구상이라 지금 코드 상태와 다를 수 있다. 큰 방향을 바꿔야 할 것 같으면 먼저 알리고 진행할 것.

## 기술 스택 및 제약

- Swift, iOS 앱 (SwiftUI)
- 오디오 캡처: AVAudioEngine, AVAudioSession
- 피치 검출: YIN 알고리즘 직접 구현 (Accelerate/vDSP 기반)
  - 서드파티 피치 검출 라이브러리(AudioKit PitchTap 등) 사용 금지 — 학습 목적상 직접 구현이 원칙. 정확도 비교용 참고 자료로만 사용 가능.
- 데이터 저장: SwiftData
- 서버/백엔드 없음 — 전 과정 온디바이스 처리가 기본 원칙
- 외부 유료 API(Klangio 등)는 v1 범위에서 사용하지 않음

## 현재 단계: 채점 재도입 + 악보 4성부 + 기록 탭 개편 완료(136절) — 다음은 실기기 확인, 이어서 리듬/채보 정확도

지금 살아있는 파이프라인: `AudioCapture`(마이크) → `QuickRecordView`(최대 60초 녹음) → `MelodySegmenter`(배치 분석, 음표별 음높이/시작시각/길이 추출) → `RecordingAnalyzer`(`KeyDetector`로 조성 판별 + `ChordGenerator`로 스텝별 화음 계산) → `MelodyStep` 배열(harmony 포함) → 세 갈래로 갈린다:

1. **악보** — `VexFlowScorePayload`(순수 함수, 페이로드 조립) → `VexFlowScoreView`(WKWebView + VexFlow). 136절부터 **멜로디/5도/3도/베이스 4성부**를 그린다(위에서 아래로 음높이 순서, `ChordGenerator`가 그 순서를 수학적으로 보장).
2. **재생** — 조작부(`captureHero`)의 **"내 목소리로 화음 듣기" 버튼 하나**(WORLD, `VoiceHarmonyTrackBuilder`) + 항상 펼쳐진 성부별 4행(각 행에 솔로 재생+뮤트 토글).
3. **채점** — `PracticeView+Scoring.swift`(항상 펼쳐진 카드): 성부 선택 → "먼저 들어보기"(솔로 재생 재사용) → "따라 부르기"(재생을 전부 끊고 마이크만) → 중지 시 `MelodySegmenter`로 다시 분석해 `HarmonyPracticeScorer`(편집거리 정렬)로 배치 채점 → `PracticeSession`/`PracticeAttempt`로 저장 → 기록 탭.

`TonePlayer`는 녹음 전 "첫음 잡기" 참고음 재생에만 쓰인다.

**채점에서 지켜야 할 전제**: 재생(듣기)과 녹음(부르기)은 **시간상 절대 겹치지 않는다**. 동시에 하면 스피커로 낸 화음이 마이크로 되돌아와 "사용자가 부른 음"으로 잘못 채점되는데, 그걸 막으려면 이어폰을 강제하거나 오디오 세션을 `.voiceChat`(하드웨어 에코 캔슬링)으로 바꿔야 한다 — 후자는 132절까지 튜닝해온 녹음 품질(YIN 정확도, WORLD 분석)에 영향이 갈 수 있다. 이 전제 덕분에 `startCaptureAfterPermissionGranted`의 피드백 가드와 오디오 세션 설정을 하나도 안 건드렸다. **앞으로 "재생하면서 채점" 같은 요구가 나오면 이 트레이드오프부터 다시 확인할 것.**

실기기 검증(121~122절)에서 "따다다닥 끊김"(화음이 멜로디 음마다 바뀌는데 매번 무음까지 페이드했던 게 원인 — 진짜 크로스페이드로 수정)과 "지지직"(짧은 음 램프 중첩 + 화음 재생 음량이 스피커 재생 한계에 가까웠던 것으로 추정 — 램프 겹침 방지+음량 하향으로 개선)을 순서대로 고쳤다. 이어서 실제 노래로 "화음이 애매하다" 재제보 — 콘솔 로그로 진단한 결과 화음 계산 자체는 정확했고, 원인은 `ChordGenerator` v1(멜로디 음마다 새 근음)의 화성 모델 한계로 확인(v2 재검토는 나중 과제로 미룸).

**123절, 2단계 1차 시도(WSOLA)**: `SynthesizedHarmonyTrackBuilder`와 같은 크로스페이드 구조 위에 목소리 피치시프트(WSOLA)를 얹은 `VoiceHarmonyTrackBuilder` 추가, "내 목소리로 화음" 버튼 추가. 실기기 청취 결과 "둘 다 어색하네"(합성음/WSOLA)로 확인 — 화음이 애매하다는 재제보를 로그로 진단해 화성 모델(v1) 한계임도 별도로 확인(코드 버그 아님, 나중 과제).

**124~127절, WORLD로 교체(성공)**: 웹서비스/오픈소스 라이선스 조사(상업용 iOS에 안전한 건 SoundTouch/WORLD) 후, 사용자 요청대로 **한번에 안 넣고 3단계로 나눠** WORLD 보코더를 복원 — 1단계(벤더링+빌드 확인) → 2단계(Swift 래퍼+합성 신호 피치 정확도 유닛테스트) → 3단계(`VoiceHarmonyTrackBuilder` 연결), 매 단계 빌드+테스트로 확인. 실기기 청취 결과 **"아까보다 훨씬 자연스럽네"로 확인(127절)** — WSOLA 대비 뚜렷한 개선.

**128절, 멜로디+화음 자연스러움 7단계 다듬기**: "딱딱 끊긴다" 제보 → (1)멜로디도 세그먼트로 잘려 미인식 구간이 무음 되던 문제를 원본 패스스루로, (2)화음 크로스페이드 확장분이 이웃 음을 침범하던 문제를 자기반사(`VoiceHarmonyTrackBuilder.mirrorExtendedSlice`)로 수정 → (3~4)포먼트 차이(0.95/1.05, 95절보다 보수적) 재도입 + 화음 3성부 backingGain(0.65)으로 멜로디 강조 → (5~6)성부별 솔로 재생 버튼 복구, 재생 버튼 전체가 실수로 악보 카드 안에 있던 걸 조작부(`captureHero`)로 재배치, 재생 중 성부 뮤트(즉시 재시작 방식, 세대 토큰으로 경쟁 상태 방지) → (7)크로스페이드 20ms→40ms 확대. 유닛테스트 146개, 매 단계 실기기 확인, "잘 작동해"/"괜찮네"로 마무리.

**129~131절, "전체 한 번 분석+F0곡선 재합성" 구조 개선(완료)**: `HarmonyUpWorldBridge.cpp`를 analyze/synthesize 핸들 기반 API로 분리(129절) → `VoiceHarmonyTrackBuilder`를 F0 곡선 조립+단일 재합성 방식으로 재작성, 세그먼트별 개별분석+진폭 크로스페이드 이어붙이기 구조 완전 제거(130절, 인접 스텝은 F0 선형보간 글리산도로 전환) → 실기기 청취 **"전보다 자연스러워서 좋다"로 목표 달성 확인**(131절).

**132절, 보코더 음질(기계음) 개선 1차 실험**: 남은 "기계음" 느낌에 대해 WORLD `D4C`의 `threshold`(비주기성 추정 임계값, 기본 0.85)를 화음 성부 분석에서만 낮춰봄 — 0.0(지름길 완전히 끔)은 "지지직" 잡음이 생겨 과했고, 중간값 **0.5로 절충 확정**("나쁘지 않아").

**133절, 다음 과제 — 화성 모델 v2 부활 검토(미착수)**: 이어서 "화음이 고정된 느낌, 멜로디 느낌이 강한 화음이면 좋겠다"는 피드백 → AskUserQuestion으로 "화음 진행 자체가 단순반복"으로 구체화. 원인은 현재 v1(101~102절)이 멜로디 음마다 그 음 자신을 근음으로 매번 새로 화음을 계산해서 사실상 병행진행에 가깝기 때문 — 51절 v2(HMM+Viterbi, 코드가 문맥에 맞게 여러 음에 걸쳐 유지)를 되살리는 방향을 제안, 구두 합의만 하고 세션 종료(저장 후 `/clear`). **119절에서 이미 "101절의 v1 회귀 이유(박자 안 맞음)가 실은 크로스페이드 버그였을 가능성"을 진단해뒀고, 그 버그는 121·128~132절에서 전부 해결됨** — 다음 세션 시작 시 이 제안을 다시 확인받고 착수할 것(51절 방출점수/전이점수/Viterbi 설계를 지금 배치 구조에 맞게 재구현 필요, git 그대로 복원은 어려움).

**134절, 화성 모델 v2 재구현 완료**: 다음 세션 시작 시 AskUserQuestion으로 재확인("바로 착수") 후 진행. `ChordGenerator.harmonizeSequence` 내부를 51절 설계(방출점수+전이점수+Viterbi DP/backtrace) 기준으로 새로 작성 — 시그니처는 그대로, 온음계 밖 음은 "직전 유효 화음 이어받기" 계약도 그대로 유지(방출점수만 중립 0으로 둬서 코드 흐름은 안 끊기게). 테스트를 손으로 미리 계산해 만들었다가 두 번 틀렸다(C→F, C-E-G가 각각 공통 코드(IV/I)로 설명 가능해서 "코드 유지"가 오히려 정답이었음) — 실제 실행 결과 기준으로 재설계해 통과시켰다(`ChordGeneratorTests` v1 전용 테스트 교체, `MelodySessionTests` 기대값 수정). 유닛테스트 152개 아이패드 시뮬레이터 통과. **다음: 실기기(Ian) 청취 검증** — "화음이 고정된 느낌"이 실제로 해소됐는지 확인 필요.

**135절, 재생 조작부 UI 재설계(Claude Design 하이파이 목업 → 실제 코드 반영)**: 연습 탭 핵심 플로우를 Claude Design 캔버스로 먼저 시안화(아트보드 8장)한 뒤 승인받고 실제 SwiftUI에 반영. 버튼 11개가 항상 다 보이던 조작부를 **"내 목소리로 화음 듣기" 버튼 하나 + 항상 펼쳐진 성부별 4행**으로 최종 정리 — 합성음 전용 "화음 듣기"(120절 `toggleHarmonyPlayback`/`harmonyPlayer`)와 원본 전용 재생(`togglePlayback`/`recordingPlayer`)이 둘 다 UI에서 빠지며 뷰 레이어에서 제거됐다(엔진 타입 `SynthesizedHarmonyTrackBuilder`/`RecordingPlayer` 자체는 재사용 가능성 때문에 유지). 아이패드 2단 스플릿의 높이·색·패딩 정렬을 실기기 스크린샷으로 3라운드 다듬음 — 마지막엔 순수 Python PNG 디코더를 즉석에서 짜서 픽셀 단위로 재본 끝에 "**`QuickRecordView`가 이미 카드인 컬럼 안에서 자기 카드를 또 그려 패딩이 이중이 된 것**"이 원인임을 찾아 `showsCardBackground` 파라미터로 해결. 녹음 애니메이션도 다듬음(REC 펄스 점 신규, 파형 막대 스프링 전환, 정지 버튼 헤일로에 약한 숨쉬기 추가 — 셋 다 Reduce Motion 존중). 유닛테스트 152개 유지. **다음: 실기기 재확인** — 마지막 변경(원본 제거+상시 펼침+애니메이션)은 아이패드 연결 끊김으로 실기기 확인 못 함.

**136절, 세 단계로 진행(전부 완료, 실기기 확인 대기)**: (1) 악보 4성부 복원 + `RhythmQuantizer.measureBreaks`가 4/4 표기에 5박을 담던 버그 수정, 페이로드 조립을 순수 타입 `VexFlowScorePayload`로 분리. 악보 라이브러리 대안(OSMD 등)도 조사했지만 **교체는 답이 아니다** — OSMD는 VexFlow 위에 얹은 MusicXML 파서라 렌더링 엔진이 같고, "음 개수/박자가 이상하다"는 증상은 렌더링이 아니라 앞단(`MelodySegmenter` 채보 / `RhythmQuantizer` 리듬)의 문제다. (2) 채점 재설계 — `HarmonyPracticeScorer`(편집거리 정렬, DTW가 아닌 이유는 누락/추가를 셀 수 있어야 해서). (3) 기록 탭 세션 단위 개편 + `PracticeStatistics`(스트릭/자주 틀리는 음/음정 편향). 유닛테스트 206개.

## 이후 작업 (2026-08-25 기준)

### ⓪ (완료, 2026-08-25 159절) 채점 결과를 악보 위에 색으로

텍스트 12줄을 없애고 악보 음표를 칠하는 것으로 바꿨다 — 자세한 배경은 `docs/CONCEPTS.md`
159절. 착수 전에 적어뒀던 함정 둘은 이렇게 풀렸다.

1. **인덱스 공간 세 개** → 순수 타입 `ScoringColorPayload`가 전부 흡수한다(원본 스텝 인덱스를
   공용 화폐로 삼아 잇는다). 성부 행 판정은 `VexFlowScorePayload.drawnVoices`로 뽑아 `build`와
   한 곳에서 공유한다 — 이 판정이 두 벌로 갈라지면 칠할 행이 조용히 어긋난다
2. **재생 하이라이트 충돌** → 되돌릴 색을 음표마다 `data-base-color` 속성에 기억시킨다
   (`render.js`의 `setBaseColor`/`baseColorOf`)

**남은 것**: 아이폰(컴팩트)에서는 채점 카드가 악보보다 위라 결과를 보려면 스크롤해야 색칠된
악보가 보인다. 실기기에서 답답하면 결과가 나올 때 악보 카드로 자동 스크롤하는 것을 검토할 것
(`ScrollViewReader`와 `sheetMusicCard` 앵커는 이미 있다).

### ① 실기기 확인 — 다른 작업의 전제 (가장 큰 리스크)

**155~158절 전체가 미검증이다.** 악보 대조 축 4일치 작업이 시뮬레이터 테스트만 통과한 상태다.

- 악보 **있는** 흐름 / **없는** 흐름 둘 다 (없는 쪽이 예전 그대로인지가 회귀 확인의 핵심)
- **사진** 입력 / **파일** 입력 둘 다 — 사진은 실제 종이로 찍어본 적이 없다(렌더 이미지로만 검증)
- 세 단계 UI 흐름(녹음 → 비교 관문 → 화음)이 실제로 자연스러운지, ②에서 악보 붙이기가 번거롭지 않은지
- 교정이 실제로 A#3 오탐을 잡는지

136~142절(채점 흐름, 기록 탭, 성부 순서)과 134절 화성 v2 청취도 아직 정식 확인 전이다.

### ② 채보 정확도 — 실기기 로그가 있어야 착수 가능

악보를 붙이면 155절 교정이 받아내지만, **악보 없이 부를 때는 그대로 남아 있는 문제**다.

- **A#3 오탐**(G장조인데 조성 밖 음이 악보에 남음) — 다음엔 흡수 **판단**은 건드리지 말고
  최종 라벨만 바꿀 것(149절에 판단을 건드렸다가 회귀했다)
- **채보가 반음 위로 번짐** — 그 녹음들은 입력 진폭 0.0070~0.0113으로 잘 나올 때(0.0118)보다
  낮았다. **로그의 원본 최대 진폭부터 볼 것**
- 코드에서 보이는 후보: `minimumNoteDuration`(0.18초), `maxPortamentoSemitones`(1), YIN 옥타브 오검출

### ③ 화음 음역 — 147절부터 이월

낮은 멜로디(D3 등)에서 3성부를 전부 멜로디 아래 두는 문제. 화음이 너무 낮게 깔린다.

### ④ 첫 사용자 안내 (UI 크리틱 P1)

"3도"가 뭔지, "cent"가 뭔지 화면 어디에도 없다. `PRODUCT.md` 원칙5("소스코드를 모르는 첫
사용자도 화면만 보고 따라갈 수 있어야")와 정면으로 어긋나고, 페르소나는 화성 이론 지식이
없는 사람으로 정의돼 있다.

*(정보 밀도 P1 — 결과 화면 탭 타깃 14~15개 — 는 158절 세 단계 분리로 해소됐다.)*

### ⑤ 접근성 (P2) — 1단계 탐지까지 완료

2026-08-25에 현황만 탐지했다(코드 수정 없음). 연습 탭은 `Label(...)`을 많이 써서 최악은
면했고 Reduce Motion도 6곳 대응돼 있지만, **`PracticeView+Scoring`과 `SessionDetailView`는
accessibility가 하나도 없다**(버튼 4개·3개). SF Symbol 크기 하드코딩도 남아 있다.

착수한다면 3단계로 나눌 것: 탐지(수정 금지) → 파일 단위 진단(JSON 고정 출력) → 한 항목씩 패치
(레이아웃·뷰 계층·비즈니스 로직 변경 금지, diff만, 확신 없으면 TODO).

### ⑥ 사진 OMR의 알려진 한계 (156절)

- **높은음자리표로 가정한다** — 낮은음자리표 악보는 전부 틀리게 읽는다(조옮김으로도 보정 불가)
- 곡 중간 임시표(#/♭/♮)는 안 읽는다 — 조표만 본다
- 여러 성부가 겹친 악보(피아노 반주)에서 위쪽 성부만 얻는다는 보장이 없다
- 임계값 넷(조옮김 신뢰도 0.35 / 일치율 0.5 / 스냅 한도 2반음 / 짧은음 0.28초)이 전부 첫 값이다

### ⑦ 정리 대상 (작음)

- **고아 코드**: `PitchMeterView`, `PracticeSummary`는 새 채점 흐름에서 안 쓰인다. 지울지 결정
- **`onSeekToStep`이 여전히 no-op** — 악보를 탭해도 그 지점부터 재생되지 않는다
  (`RecordingPlayer`에 탐색 기능이 없다)
- **무음 채점도 0%로 저장된다** — 아무 소리 없이 "그만 부르기"를 눌러도 기록에 남는다
- **채점 녹음에는 파형도 60초 상한 표시도 없다** — 멜로디 녹음과 비대칭

## 코딩 컨벤션

- 신호처리 핵심 로직에는 "왜 이렇게 했는지"를 짧은 주석으로 남길 것 (자기상관 윈도우 크기, 임계값 선택 근거 등)
- 매직 넘버 대신 명명된 상수 사용
- 신호처리 함수는 유닛 테스트가 가능하도록 순수 함수로 분리 — **View의 `@State`에 묶인 메서드 안에 신호처리/조합 로직을 직접 두지 말 것.** 화음 오디오 파이프라인(`harmonizedTrack`)이 한동안 `PracticeView` 확장 메서드로 있었는데, 그 조합 로직(간격 처리, 여러 음 이어붙이기)에 실제 버그(트랙 길이가 미묘하게 어긋나 "노래가 빠를수록 화음이 밀림")가 있었지만 유닛테스트가 없어서 실기기 청취로만 드러났다 — `PitchShifter`/`VoiceDoubler`/`AudioGain`처럼 이미 옆에 있던 순수 함수들은 같은 기간 내내 한 번도 실패하지 않았다. 이후 `HarmonyTrackBuilder`(PitchEngine)로 뽑아 유닛테스트를 붙였다 — 앞으로 이 자리를 다시 손댈 때는 그 테스트에 불변식(주로 길이 보존)을 먼저 추가해 통과시킨 뒤 실기기로 넘어갈 것.
- 오디오/신호처리 쪽을 실기기 청취로 튜닝할 때는 한 커밋에 여러 개념을 동시에 바꾸지 말 것 — 어느 변경이 무엇을 고쳤는지/깼는지 추적이 어려워진다. 복잡한 해법(예: 크로스페이드, 여러 스텝 묶기)보다 단순한 해법을 먼저 시도할 것 — 복잡도 자체가 새 버그의 원인이 될 수 있다(2026-08-20, 화음 파이프라인 7라운드 조정에서 실제로 겪음).

## 작업 방식

- 작업자는 iOS 개발은 익숙하지만 DSP는 이번에 처음 제대로 배우는 중이다. 코드만 작성하지 말고, 핵심 알고리즘 결정마다 왜 그렇게 했는지 1~2문장으로 짧게 설명할 것.
- 큰 구조 변경이나 새 의존성 추가 전에는 먼저 접근 방식을 요약해서 확인받을 것.
- 컴포넌트/기능을 하나씩 순서대로 만들고, 매 단계마다 결과를 확인받은 뒤 다음으로 넘어간다(한 번에 몰아서 만들지 않는다).
- 새 컴포넌트를 만들 때마다 그 안에서 쓴 기술/개념을 `docs/CONCEPTS.md`에 이해하기 쉽게 정리해서 이어붙인다.

## 커밋 컨벤션 (2026-08-25 확정)

junCook(Junction2026 team15) 레포와 **같은 규칙**을 쓴다 — 두 레포를 오가며 작업하므로 규칙이
갈라지지 않게 맞춘다.

- 제목은 `<type>: <subject>` — **한글로 간결하게, 마침표를 붙이지 않는다**
- type: `feat`(기능) `fix`(버그) `refactor`(구조) `test`(테스트) `docs`(문서)
  `build`(빌드·의존성·프로젝트 구성) `chore`(잡무)
- **한 커밋에는 하나의 논리적 변경만** — 성격이 다른 변경은 커밋을 나눈다
- **본문은 지금까지처럼 상세히 쓴다.** 이 프로젝트는 "왜 그렇게 했는지"가 자산이라
  (신호처리 임계값 근거, 실기기에서 드러난 증상, 되돌린 이유) 제목 형식만 맞추고 본문의
  깊이는 그대로 유지한다. 제목이 짧아진 만큼 본문이 더 중요해진다
- Claude 흔적(`Co-Authored-By`, 생성 푸터)은 남기지 않는다

예:

```
feat: 악보 비교를 관문으로 두고 녹음 뒤를 두 단계로

②가 관문이라는 게 핵심이다 — 바로 화음으로 가지 않고 교정 결과를 눈으로
확인하고 승인한 뒤에 넘어간다. 화음 자체는 분석 한 번에 이미 계산돼 있어서
이 전환은 무엇을 보여줄지의 문제일 뿐이다.
```

`docs:`는 `docs/CONCEPTS.md`·`PROGRESS.md`·README 갱신처럼 **문서만** 바뀐 커밋에 쓴다 —
기능 커밋에 딸린 주석은 그 기능 커밋에 포함된다.

## 작업 완료 루틴 (매번, 요청 없이도 자동으로)

작업 단위(컴포넌트 하나, 기능 하나) 하나를 마칠 때마다 아래를 전부 수행한다:

1. **`docs/CONCEPTS.md` 갱신** — 새로 쓴 기술/개념 설명 추가
2. **`docs/PROGRESS.md` 갱신** — 진행 상황 체크리스트 항목 추가. **`README.md`에는 진행 상황을 쓰지 않는다** — 링크만 둔다(2026-08-24에 다시 정리됨). 예전엔 "최근 작업 한 줄 요약"까지 갱신하는 규칙이었는데, 작업마다 한 줄씩 붙다 보니 절 번호와 화살표로 이어진 스무 줄짜리 문단이 됐다. README는 **이 프로젝트를 처음 보는 사람이 읽는 문서**라 개발 서사(무엇을 만들었다 지웠다)와 내부 절 번호는 맞지 않는다 — 앱이 무엇을 하는지, 구조가 어떤지, 어떻게 빌드하는지만 둔다. 히스토리는 `docs/PROGRESS.md`와 `docs/CONCEPTS.md`가 갖는다
3. **git commit + push** — 작업 단위별로 커밋을 나누고 원격 저장소에 푸시까지 완료
4. **Claude 메모리 갱신** — `project_harmonyup` 메모리에 이번 작업으로 달라진 진행 상황 기록

이 프로젝트는 홈 디렉토리 전체를 감싸는 상위 git 저장소와 별개로, `~/Desktop/HarmonyUp` 자체가 독립된 git 저장소다. push 전에는 항상 이 저장소 범위(`git status`가 프로젝트 파일만 보여주는지) 안에서 작업 중인지 확인할 것 — 상위 저장소로 잘못 올라가는 일이 없도록 주의.

## 세션 관리 — /compact 대신 /clear

- `/compact`(대화 요약 압축)는 쓰지 않는다 — 긴 코드/에러 로그/미세 조율 내용이 요약 과정에서 생략·왜곡되기 쉽고, 잘못 요약된 맥락을 사실로 믿고 계속 진행하면 환각(hallucination)으로 이어질 위험이 있다. 토큰 절감 효과도 크지 않다(요약본 자체도 계속 토큰을 차지해서 얼마 안 가 다시 한도에 도달).
- 대신 이 흐름을 따른다: 기능 하나가 구현되거나 버그가 잡히면 그때그때 **git commit**(+push)을 해두고, 한 단계(예: 어떤 Phase/Task)가 끝나면 고민 없이 **`/clear`**로 대화를 깨끗하게 초기화한 뒤 다음 작업을 새로 지시받는다.
- `/clear`는 대화 맥락만 비우는 것이지 소스 코드 파일이나 git 커밋 기록을 지우지 않는다 — 세션이 새로 시작해도 이 `CLAUDE.md`와 `docs/CONCEPTS.md`, git 로그만 다시 읽으면 필요한 맥락은 그대로 복원된다. 그래서 중요한 프로젝트 규칙/결정은 반드시 이 문서(또는 `docs/CONCEPTS.md`)에 남겨야 한다 — 대화 맥락에만 의존하지 말 것.
